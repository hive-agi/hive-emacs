;;; hive-mcp-cider-test.el --- -*- lexical-binding: t; -*-
;; Generated by ClojureElisp

(eval-and-compile
  (require 'clojure-elisp-runtime)
  (unless (and (boundp 'clel-runtime-version)
               (version<= "0.7.2" clel-runtime-version))
    (error "clojure-elisp-runtime %s is too old for this file (needs %s)"
           (if (boundp 'clel-runtime-version) clel-runtime-version "(pre-0.7.2)")
           "0.7.2")))
(require 'cl-lib)

;;; Code:



(require 'ert)

(require 'transient)

(require 'hive-mcp-cider)

(ert-deftest hive-mcp-cider-test-kill-session-unknown-signals nil "Killing an unregistered session is a loud error, never a silent no-op." (hive-mcp-cider-sessions-clear-all) (unwind-protect
    (should-error (hive-mcp-cider-kill-session "no-such-session-xyz") :type 'error)
  (hive-mcp-cider-sessions-clear-all)))

(ert-deftest hive-mcp-cider-test-kill-session-blank-signals nil "A blank or nil session name is a loud error." (should-error (hive-mcp-cider-kill-session "") :type 'error) (should-error (hive-mcp-cider-kill-session nil) :type 'error))

(ert-deftest hive-mcp-cider-test-kill-session-registered-succeeds nil "A registered session is killed and unregistered." (hive-mcp-cider-sessions-clear-all) (unwind-protect
    (progn
  (hive-mcp-cider-sessions-register "victim" (list :port 7999 :process nil))
  (hive-mcp-cider-kill-session "victim")
  (should (not (hive-mcp-cider-sessions-exists-p "victim"))))
  (hive-mcp-cider-sessions-clear-all)))

(provide 'hive-mcp-cider-test)
;;; hive-mcp-cider-test.el ends here
