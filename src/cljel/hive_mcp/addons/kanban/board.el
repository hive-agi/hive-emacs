;;; hive-mcp-kanban-board.el --- -*- lexical-binding: t; -*-
;; Generated by ClojureElisp

(require 'clojure-elisp-runtime)
(require 'cl-lib)
(require 'org-id)
(require 'hive-mcp-kanban-protocol)

;;; Code:



(declare-function hive-mcp-org-kanban-list-tasks "hive-mcp-org-kanban")

(declare-function hive-mcp-org-kanban-create-task "hive-mcp-org-kanban")

(declare-function hive-mcp-org-kanban-update-task "hive-mcp-org-kanban")

(declare-function hive-mcp-org-kanban-delete-task "hive-mcp-org-kanban")

(declare-function hive-mcp-org-kanban-move-task "hive-mcp-org-kanban")

(declare-function hive-mcp-kanban-standalone-load-tasks-by-status "hive-mcp-kanban-standalone")

(declare-function cider-connected-p "cider")

(declare-function cider-nrepl-sync-request:eval "cider")

(declare-function nrepl-dict-get "nrepl-dict")

(defvar hive-mcp-kanban-standalone-org-file nil)

(defface hive-mcp-kanban-column-header '((t :inherit font-lock-keyword-face :weight bold :height 1.1)) "Face for kanban column headers." :group 'hive-mcp-kanban)

(defface hive-mcp-kanban-task '((t :inherit default)) "Face for kanban task cards." :group 'hive-mcp-kanban)

(defface hive-mcp-kanban-task-highlight '((t :inherit highlight)) "Face for highlighted kanban task." :group 'hive-mcp-kanban)

(defface hive-mcp-kanban-border '((t :inherit font-lock-comment-face)) "Face for kanban board borders." :group 'hive-mcp-kanban)

(defface hive-mcp-kanban-stats '((t :inherit font-lock-doc-face)) "Face for kanban statistics." :group 'hive-mcp-kanban)

(defvar-local hive-mcp-kanban-board--tasks nil "Current board tasks organized by column.")

(defvar-local hive-mcp-kanban-board--column-positions nil "Alist of (column-name . start-pos) for navigation.")

(defvar-local hive-mcp-kanban-board--task-positions nil "Alist of (task-id . (start-pos . end-pos)) for navigation.")

(defvar hive-mcp-kanban-board-mode-map (let* ((map (make-sparse-keymap)))
    (define-key map (kbd "n") #'hive-mcp-kanban-board-next-task)
    (define-key map (kbd "p") #'hive-mcp-kanban-board-prev-task)
    (define-key map (kbd "f") #'hive-mcp-kanban-board-next-column)
    (define-key map (kbd "b") #'hive-mcp-kanban-board-prev-column)
    (define-key map (kbd "TAB") #'hive-mcp-kanban-board-next-column)
    (define-key map (kbd "<backtab>") #'hive-mcp-kanban-board-prev-column)
    (define-key map (kbd "j") #'hive-mcp-kanban-board-next-task)
    (define-key map (kbd "k") #'hive-mcp-kanban-board-prev-task)
    (define-key map (kbd "l") #'hive-mcp-kanban-board-next-column)
    (define-key map (kbd "h") #'hive-mcp-kanban-board-prev-column)
    (define-key map (kbd "RET") #'hive-mcp-kanban-board-open-task)
    (define-key map (kbd "m") #'hive-mcp-kanban-board-move-task)
    (define-key map (kbd ">") #'hive-mcp-kanban-board-move-right)
    (define-key map (kbd "<") #'hive-mcp-kanban-board-move-left)
    (define-key map (kbd "c") #'hive-mcp-kanban-board-create-task)
    (define-key map (kbd "d") #'hive-mcp-kanban-board-delete-task)
    (define-key map (kbd "e") #'hive-mcp-kanban-board-edit-task)
    (define-key map (kbd "g") #'hive-mcp-kanban-board-refresh)
    (define-key map (kbd "o") #'hive-mcp-kanban-board-open-org-file)
    (define-key map (kbd "q") #'quit-window)
    (define-key map (kbd "?") #'hive-mcp-kanban-board-help)
    map)
  "Keymap for `hive-mcp-kanban-board-mode'.")

(define-derived-mode hive-mcp-kanban-board-mode special-mode "Kanban" "Major mode for interactive kanban board.\n\n\\{hive-mcp-kanban-board-mode-map}" :group 'hive-mcp-kanban (setq-local truncate-lines t) (setq-local cursor-type 'bar) (setq-local line-spacing 0.2) (hl-line-mode 1))

(with-eval-after-load 'evil
  (evil-set-initial-state 'hive-mcp-kanban-board-mode 'emacs)
  (evil-define-key 'normal hive-mcp-kanban-board-mode-map "n" #'hive-mcp-kanban-board-next-task "p" #'hive-mcp-kanban-board-prev-task "j" #'hive-mcp-kanban-board-next-task "k" #'hive-mcp-kanban-board-prev-task "l" #'hive-mcp-kanban-board-next-column "h" #'hive-mcp-kanban-board-prev-column (kbd "RET") #'hive-mcp-kanban-board-open-task "m" #'hive-mcp-kanban-board-move-task ">" #'hive-mcp-kanban-board-move-right "<" #'hive-mcp-kanban-board-move-left "c" #'hive-mcp-kanban-board-create-task "d" #'hive-mcp-kanban-board-delete-task "e" #'hive-mcp-kanban-board-edit-task "g" #'hive-mcp-kanban-board-refresh "o" #'hive-mcp-kanban-board-open-org-file "q" #'quit-window "?" #'hive-mcp-kanban-board-help))

(defun hive-mcp-kanban-board---render ()
  "Render the kanban board in the current buffer."
  (let* ((inhibit-read-only t)
        (file hive-mcp-kanban-standalone-org-file)
        (tasks-by-status (hive-mcp-kanban-board--load-tasks file))
        (col-width 30)
        (columns '(("TODO" . "📋") ("IN-PROGRESS" . "🔄") ("IN-REVIEW" . "👀") ("DONE" . "✅")))
        (task-positions '())
        (column-positions '()))
    (erase-buffer)
    (insert (propertize "╔═════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╗\n" 'face 'hive-mcp-kanban-border))
    (insert (propertize "║                                              " 'face 'hive-mcp-kanban-border))
    (insert (propertize " KANBAN BOARD " 'face '(:inherit hive-mcp-kanban-column-header :height 1.3)))
    (insert (propertize "                                              ║\n" 'face 'hive-mcp-kanban-border))
    (insert (propertize "╠═════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣\n" 'face 'hive-mcp-kanban-border))
    (let* ((total (apply #'-plus (mapcar (lambda (col)
    (length (cdr (clel-assoc (car col) tasks-by-status)))) columns)))
        (done (length (cdr (clel-assoc "DONE" tasks-by-status))))
        (in-prog (length (cdr (clel-assoc "IN-PROGRESS" tasks-by-status))))
        (todo (length (cdr (clel-assoc "TODO" tasks-by-status)))))
    (insert (propertize "║ " 'face 'hive-mcp-kanban-border))
    (insert (propertize (format "Total: %d  │  Todo: %d  │  In Progress: %d  │  Done: %d  │  Progress: %d%%" total todo in-prog done (if (> total 0) (round (* 100 (/ (float done) total))) 0)) 'face 'hive-mcp-kanban-stats))
    (insert (make-string (- 122 (current-column)) -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions done column-positions columns tasks-by-status file in-prog col-width total todo}, :aliases {}, :refers {}, :in-tail-position? false}, :line 179, :column 15, :form \s}))
    (insert (propertize "║\n" 'face 'hive-mcp-kanban-border)))
    (insert (propertize "╠═════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣\n" 'face 'hive-mcp-kanban-border))
    (insert (propertize "║" 'face 'hive-mcp-kanban-border))
    (dolist (col columns)
    (let* ((name (car col))
        (emoji (cdr col))
        (count (length (cdr (clel-assoc name tasks-by-status))))
        (header (format " %s %s (%d) " emoji name count)))
    (push (cons name (point)) column-positions)
    (insert (propertize header 'face 'hive-mcp-kanban-column-header 'column-name name))
    (insert (make-string (- col-width (length header)) -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions emoji column-positions columns tasks-by-status name file header col-width col count}, :aliases {}, :refers {}, :in-tail-position? false}, :line 195, :column 17, :form \s}))))
    (insert (propertize "║\n" 'face 'hive-mcp-kanban-border))
    (insert (propertize "╠" 'face 'hive-mcp-kanban-border))
    (cl-dotimes (_ 4)
    (insert (propertize (make-string col-width -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions column-positions _ columns tasks-by-status file col-width}, :aliases {}, :refers {}, :in-tail-position? false}, :line 200, :column 27, :form \─}) 'face 'hive-mcp-kanban-border)))
    (insert (propertize "╣\n" 'face 'hive-mcp-kanban-border))
    (let* ((max-tasks (apply #'max 1 (mapcar (lambda (col)
    (length (cdr (clel-assoc (car col) tasks-by-status)))) columns))))
    (cl-dotimes (row (cl-min max-tasks 15))
    (insert (propertize "║" 'face 'hive-mcp-kanban-border))
    (dolist (col columns)
    (let* ((col-name (car col))
        (col-tasks (cdr (clel-assoc col-name tasks-by-status)))
        (task (nth row col-tasks)))
    (if task (let* ((title (or (cdr (clel-assoc 'title task)) "Untitled"))
        (id (cdr (clel-assoc 'id task)))
        (truncated (if (> (length title) (- col-width 4)) (clel-concat (substring title 0 (- col-width 7)) "...") title))
        (start-pos (point)))
    (insert (propertize (format " * %s" truncated) 'face 'hive-mcp-kanban-task 'task-id id 'task-data task 'column-name col-name 'mouse-face 'hive-mcp-kanban-task-highlight 'help-echo (format "Task: %s\nID: %s\nClick to open" title id)))
    (insert (make-string (- col-width (+ 3 (length truncated))) -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions start-pos row col-name column-positions task columns max-tasks tasks-by-status file title col-width col-tasks col id truncated}, :aliases {}, :refers {}, :in-tail-position? false}, :line 228, :column 27, :form \s}))
    (push (cons id (cons start-pos (point))) task-positions)) (insert (make-string col-width -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions row col-name column-positions task columns max-tasks tasks-by-status file col-width col-tasks col}, :aliases {}, :refers {}, :in-tail-position? false}, :line 230, :column 23, :form \s})))))
    (insert (propertize "║\n" 'face 'hive-mcp-kanban-border)))
    (dolist (col columns)
    (let* ((col-name (car col))
        (col-tasks (cdr (clel-assoc col-name tasks-by-status)))
        (extra (- (length col-tasks) 15)))
    (when (> extra 0)
    (insert (propertize "║" 'face 'hive-mcp-kanban-border))
    (dolist (c columns)
    (if (equal (car c) col-name) (let* ((more-text (format " ... +%d more" extra)))
    (insert (propertize more-text 'face 'font-lock-comment-face))
    (insert (make-string (- col-width (length more-text)) -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions col-name column-positions columns max-tasks tasks-by-status file extra col-width c more-text col-tasks col}, :aliases {}, :refers {}, :in-tail-position? false}, :line 244, :column 29, :form \s}))) (insert (make-string col-width -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions col-name column-positions columns max-tasks tasks-by-status file extra col-width c col-tasks col}, :aliases {}, :refers {}, :in-tail-position? false}, :line 245, :column 25, :form \s}))))
    (insert (propertize "║\n" 'face 'hive-mcp-kanban-border))))))
    (insert (propertize "╠═════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╣\n" 'face 'hive-mcp-kanban-border))
    (insert (propertize "║ " 'face 'hive-mcp-kanban-border))
    (insert (propertize "[n/p] Navigate  [h/l] Columns  [RET] Open  [>/<] Move  [c] Create  [d] Delete  [g] Refresh  [q] Quit  [?] Help" 'face 'font-lock-comment-face))
    (insert (make-string (- 122 (current-column)) -p ;; Unknown node: {:op :unknown, :env {:ns hive-mcp-kanban-board, :locals #{inhibit-read-only task-positions column-positions columns tasks-by-status file col-width}, :aliases {}, :refers {}, :in-tail-position? false}, :line 254, :column 13, :form \s}))
    (insert (propertize "║\n" 'face 'hive-mcp-kanban-border))
    (insert (propertize "╚═════════════════════════════════════════════════════════════════════════════════════════════════════════════════════════╝\n" 'face 'hive-mcp-kanban-border))
    (setq hive-mcp-kanban-board--tasks tasks-by-status)
    (setq hive-mcp-kanban-board--column-positions (nreverse column-positions))
    (setq hive-mcp-kanban-board--task-positions (nreverse task-positions))
    (goto-char (point-min))
    (hive-mcp-kanban-board-next-task)))

(defun hive-mcp-kanban-board---load-tasks (file)
  "Load tasks from FILE grouped by status."
  (if (and (fboundp 'cider-connected-p) (cider-connected-p)) (hive-mcp-kanban-board--load-tasks-cider file) (hive-mcp-kanban-standalone-load-tasks-by-status file)))

(defun hive-mcp-kanban-board---load-tasks-cider (file)
  "Load tasks from FILE using CIDER/Clojure."
  (let* ((code (format "(do (require '[hive-mcp.org-clj.parser :as p])\n                           (require '[hive-mcp.org-clj.query :as q])\n                           (let [doc (p/parse-document (slurp \"%s\"))]\n                             {:todo (q/find-todo doc)\n                              :in-progress (q/find-in-progress doc)\n                              :in-review (q/find-by-status doc \"IN-REVIEW\")\n                              :done (q/find-done doc)}))" file))
        (result (cider-nrepl-sync-request:eval code))
        (value (nrepl-dict-get result "value")))
    (when value
    (let* ((data (read value)))
    (backquote (("TODO" - (mapcar #'-convert-task (plist-get data :todo))) ("IN-PROGRESS" - (mapcar #'-convert-task (plist-get data :in-progress))) ("IN-REVIEW" - (mapcar #'-convert-task (plist-get data :in-review))) ("DONE" - (mapcar #'-convert-task (plist-get data :done)))))))))

(defun hive-mcp-kanban-board---convert-task (task-plist)
  "Convert TASK-PLIST from Clojure to alist."
  (backquote ((id - (plist-get task-plist :id)) (title - (plist-get task-plist :title)) (status - (plist-get task-plist :status)) (level - (plist-get task-plist :level)))))

(defun hive-mcp-kanban-board-next-task ()
  "Move to next task in board."
  (interactive)
  (let* ((next-pos nil))
    (save-excursion
    (forward-char 1)
    (while (and (not (eobp)) (not (get-text-property (point) 'task-id)))
    (forward-char 1))
    (when (get-text-property (point) 'task-id)
    (setq next-pos (point))))
    (when next-pos
    (goto-char next-pos))))

(defun hive-mcp-kanban-board-prev-task ()
  "Move to previous task in board."
  (interactive)
  (let* ((prev-pos nil))
    (save-excursion
    (backward-char 1)
    (while (and (not (bobp)) (not (get-text-property (point) 'task-id)))
    (backward-char 1))
    (when (get-text-property (point) 'task-id)
    (setq prev-pos (point))))
    (when prev-pos
    (goto-char prev-pos))))

(defun hive-mcp-kanban-board-next-column ()
  "Move to next column."
  (interactive)
  (let* ((current-col (get-text-property (point) 'column-name))
        (cols '("TODO" "IN-PROGRESS" "IN-REVIEW" "DONE"))
        (next-col (cadr (member current-col cols))))
    (when next-col
    (goto-char (point-min))
    (while (and (not (eobp)) (not (equal (get-text-property (point) 'column-name) next-col)))
    (forward-char 1))
    (when (equal (get-text-property (point) 'column-name) next-col)
    (point)))))

(defun hive-mcp-kanban-board-prev-column ()
  "Move to previous column."
  (interactive)
  (let* ((current-col (get-text-property (point) 'column-name))
        (cols '("DONE" "IN-REVIEW" "IN-PROGRESS" "TODO"))
        (prev-col (cadr (member current-col cols))))
    (when prev-col
    (goto-char (point-min))
    (while (and (not (eobp)) (not (equal (get-text-property (point) 'column-name) prev-col)))
    (forward-char 1))
    (when (equal (get-text-property (point) 'column-name) prev-col)
    (point)))))

(defun hive-mcp-kanban-board-open-task ()
  "Open task at point in org file."
  (interactive)
  (when-let-star (list id (get-text-property (point) 'task-id)) (let* ((marker (org-id-find id 'marker)))
    (when marker
    (switch-to-buffer (marker-buffer marker))
    (goto-char marker)
    (if (fboundp 'org-fold-show-entry) (org-fold-show-entry) (with-no-warnings (org-show-entry)))))))

(defun hive-mcp-kanban-board-move-right ()
  "Move task at point to next status column."
  (interactive)
  (hive-mcp-kanban-board--move-task 1))

(defun hive-mcp-kanban-board-move-left ()
  "Move task at point to previous status column."
  (interactive)
  (hive-mcp-kanban-board--move-task -1))

(defun hive-mcp-kanban-board---move-task (direction)
  "Move task at point in DIRECTION (+1 or -1).\nUses the addon's unified API for auto-sync and agent tracking."
  (when-let-star (list id (get-text-property (point) 'task-id) current-col (get-text-property (point) 'column-name)) (let* ((cols '("TODO" "IN-PROGRESS" "IN-REVIEW" "DONE"))
        (current-idx (cl-position current-col cols :test #'equal))
        (new-idx (+ current-idx direction))
        (new-col (and (>= new-idx 0) (< new-idx 4) (nth new-idx cols))))
    (when new-col
    (let* ((vibe-status (hive-mcp-kanban-protocol--org-to-vibe-status new-col)))
    (hive-mcp-org-kanban-move-task id vibe-status))
    (hive-mcp-kanban-board-refresh)
    (message "Moved task to %s" new-col)))))

(defun hive-mcp-kanban-board-move-task ()
  "Move task at point to a new status (interactive).\nUses the addon's unified API for auto-sync and agent tracking."
  (interactive)
  (when-let-star (list id (get-text-property (point) 'task-id)) (let* ((new-status (completing-read "Move to: " '("TODO" "IN-PROGRESS" "IN-REVIEW" "DONE") nil t))
        (vibe-status (hive-mcp-kanban-protocol--org-to-vibe-status new-status)))
    (hive-mcp-org-kanban-move-task id vibe-status)
    (hive-mcp-kanban-board-refresh)
    (message "Moved task to %s" new-status))))

(defun hive-mcp-kanban-board-create-task ()
  "Create a new task."
  (interactive)
  (let* ((title (clel-read-string "Task title: ")))
    (hive-mcp-org-kanban-create-task title)
    (hive-mcp-kanban-board-refresh)
    (message "Created task: %s" title)))

(defun hive-mcp-kanban-board-delete-task ()
  "Delete task at point.\nUses the addon's unified API for auto-sync and agent tracking."
  (interactive)
  (when-let-star (list id (get-text-property (point) 'task-id) task (get-text-property (point) 'task-data)) (when (yes-or-no-p (format "Delete task '%s'? " (cdr (clel-assoc 'title task))))
    (hive-mcp-org-kanban-delete-task id)
    (hive-mcp-kanban-board-refresh)
    (message "Task deleted"))))

(defun hive-mcp-kanban-board-edit-task ()
  "Edit task title at point.\nUses the addon's unified API for auto-sync and agent tracking."
  (interactive)
  (when-let-star (list id (get-text-property (point) 'task-id) task (get-text-property (point) 'task-data)) (let* ((old-title (cdr (clel-assoc 'title task)))
        (new-title (clel-read-string "New title: " old-title)))
    (unless (string= old-title new-title)
    (hive-mcp-org-kanban-update-task id :title new-title)
    (hive-mcp-kanban-board-refresh)
    (message "Task updated")))))

(defun hive-mcp-kanban-board-refresh ()
  "Refresh the kanban board."
  (interactive)
  (when (eq major-mode 'hive-mcp-kanban-board-mode)
    (let* ((pos (point)))
    (hive-mcp-kanban-board--render)
    (goto-char (cl-min pos (point-max))))))

(defun hive-mcp-kanban-board-open-org-file ()
  "Open the kanban org file."
  (interactive)
  (find-file hive-mcp-kanban-standalone-org-file))

(defun hive-mcp-kanban-board-help ()
  "Show help for kanban board."
  (interactive)
  (message "Kanban: n/p=nav tasks, h/l=nav cols, RET=open, >/<= move, c=create, d=delete, g=refresh, q=quit"))

(defun hive-mcp-kanban-board-open-board ()
  "Open interactive kanban board buffer."
  (interactive)
  (let* ((buf (get-buffer-create "*Kanban Board*")))
    (with-current-buffer buf
    (hive-mcp-kanban-board-mode)
    (hive-mcp-kanban-board--render))
    (switch-to-buffer buf)))

(provide 'hive-mcp-kanban-board)
;;; hive-mcp-kanban-board.el ends here
