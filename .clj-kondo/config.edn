;; Macro configs for hive-di / hive-dsl / hive-schemas / hive-spi are NOT
;; declared here — those libraries export their own, and CI imports them with
;; `--dependencies --copy-configs`. A local :lint-as would shadow the richer
;; exported hook and reintroduce the findings the hook exists to prevent.
{:config-in-call
 {ansatz.core/theorem
  {:linters {:unresolved-symbol {:level :off}
             :invalid-arity {:level :off}}}

  typed.clojure/ann
  {:linters {:unresolved-symbol {:level :off}}}}}
